# ==================== LLM Configuration (Required) ====================
# OpenAI compatible API Key
API_KEY=your-api-key-here
# API Base URL (e.g., OpenAI, DashScope, or custom proxy)
BASE_URL=https://api.openai.com/v1
# LLM Model name (e.g., gpt-4, gpt-3.5-turbo, qwen-flash)
LLM_MODEL=gpt-3.5-turbo

# LLM Generation Parameters (Optional)
LLM_TEMPERATURE=0.1
LLM_MAX_TOKENS=14000

# Agent Configuration (Optional)
AGENT_RECURSION_LIMIT=50

# ==================== Embedding Configuration ====================
# Embedding provider: jina | qwen | bge (default: jina)
EMBEDDING_PROVIDER=jina
# Embedding API URL
EMBEDDING_API_URL=http://localhost:8603/v1/embeddings
# Embedding API Key (Optional, not required for local Jina deployment)
# EMBEDDING_API_KEY=your-embedding-api-key
# Embedding Model Name (Optional, uses default if not specified)
# EMBEDDING_MODEL_NAME=jina-embeddings-v2-base-zh

# ==================== Milvus Configuration ====================
# Milvus service address
MILVUS_URI=http://localhost:19530
# Vector similarity metric: COSINE | L2 | IP
MILVUS_METRIC_TYPE=COSINE

# ==================== MySQL Configuration ====================
# MySQL database host
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=your_database_name
MYSQL_USER=your_username
MYSQL_PASSWORD=your_password
